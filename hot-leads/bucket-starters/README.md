Use these files as template-ready starters for leads `23-30`.

Workflow:

1. Open the matching bucket JSON.
2. Replace the placeholder company details:
   - `business_name`
   - `city`
   - `phone_display`
   - `rating`
   - `review_count`
   - `website_url`
   - `source_urls`
   - `gallery_images`
   - `review_quotes`
3. Keep the bucket-specific angle and SuperSite copy unless you want to tune it.
4. Publish with:

```bash
python3 /home/shark/jawsai911-site-fixed/hot-leads/quick_swap_demo.py /home/shark/jawsai911-site-fixed/hot-leads/bucket-starters/<file>.json
```
