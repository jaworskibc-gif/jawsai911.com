These are real-company SuperSite starter files for leads `21-30`.

What is already filled in:
- company name
- city
- phone
- rating
- review count
- text line
- call script
- source links
- SuperSite structure
- shared pool transformation clip

What still needs final swap-in before sending:
- `gallery_images` if you want company-specific photos instead of defaults
- `video_poster_url` if you want a company-specific poster
- `review_quotes` with 3 actual review snippets

Publish any lead with:

```bash
python3 /home/shark/jawsai911-site-fixed/hot-leads/quick_swap_demo.py /home/shark/jawsai911-site-fixed/hot-leads/real-leads-21-30/<file>.json
```
